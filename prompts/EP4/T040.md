T057 — Compute and persist aggregate video job metrics
+Epic: EP4 | Feature: F4.5 | Milestone: M4
+TASK METADATA
+Risk Level: Medium — feeds directly into the frontend's metrics panel (T068) and later analytics rollups (T080); incorrect aggregation misleads users about performance.
+AI Difficulty: Medium.
+Expected LOC: ~60
+Expected Files Created: 0 | Expected Files Modified: 1
+CURRENT REPOSITORY STATE
+Completed: T001–T056. Relevant modules: /worker/app/tasks/video_task.py (T055/T056), /inference-engine/pipeline/video_pipeline.py (T022/T023, per-frame FPS/timing already logged internally), /inference-engine/pipeline/image_pipeline.py (T021, per-class stats pattern to mirror for video aggregation).
+PROJECT CONTEXT
+Closes Section 8.2's video-job metrics field requirement ({fps, avgInferenceMs, classDistribution}), completing the contract T050 defined.
+TASK OBJECTIVE
+Have the worker compute and persist the aggregate metrics object (average FPS, average inference time, overall class distribution) once video processing completes.
+INPUT CONTEXT
+Current files: video_task.py (T055/T056), video_pipeline.py (T022–T024). Reference: Section 8.2 (metrics shape).
+FILES TO CREATE
+None.
+FILES TO MODIFY
+/worker/app/tasks/video_task.py
+IMPLEMENTATION REQUIREMENTS
+Aggregate the per-frame timing/FPS values collected during T022–T024's processing loop into a single average FPS and average inference-time-in-ms value; aggregate per-frame class pixel statistics into an overall class-distribution percentage breakdown (reusing the vectorized approach pattern from T021, not a per-pixel loop); write this metrics object onto the job record at the same point status is set to "completed" (T055).
+ENGINEERING STANDARDS
+No per-pixel Python loops for aggregation. Metrics object field names match Section 8.2 exactly (no renaming).
+CONSTRAINTS
+Do not modify video_pipeline.py's per-frame logic — only consume its already-available per-frame outputs. Do not modify T056's retry logic.
+
+ACCEPTANCE CRITERIA
+
+ A completed video job's GET response includes metrics matching the Section 8.2 shape.
+EXIT CRITERIA
+✓ Integration test confirms metrics populated and correctly shaped on completion ✓ No lint errors ✓ N/A docs ✓ Commit ready
+TESTING REQUIREMENTS
+Integration test: process a short sample video end-to-end, then fetch the completed job via GET /api/v1/jobs/{jobId} and confirm the metrics object is present with plausible fps, avgInferenceMs, and a classDistribution that sums to ~100%.
+
+OUTPUT FORMAT
+
+Summary 2. Files Created 3. Files Modified 4. Why each file changed 5. Implementation Notes 6. Testing Performed 7. Known Limitations 8. Suggested Git Commit Message 9. Next Recommended Task
+

T030 — Benchmark and document final inference latency against NFR1
Epic: EP2 | Feature: F2.8 | Milestone: M2
TASK METADATA
Risk Level: High — this is the pass/fail determination for NFR1, the project's core latency requirement; it is cited directly in the final report (T109).
AI Difficulty: N/A (human-executed) / Easy for AI script-generation portion.
Expected LOC: ~40 (benchmark-runner script)
Expected Files Created: 1 | Expected Files Modified: 0
CURRENT REPOSITORY STATE
Completed: T001–T029 (TorchScript export human-verified via T028; FP16 path human-verified via T029). Relevant modules: image_pipeline.py, video_pipeline.py, deeplabv3_backend.py.
PROJECT CONTEXT
Final Epic 2 checkpoint: determines whether the optimized pipeline meets NFR1 (<3s image inference) on both GPU and CPU, closing out Feature F2.8.
HUMAN EXECUTION REQUIRED
A human must run the AI-generated benchmark script on both available GPU and CPU hardware, record the resulting latency numbers, and confirm the explicit pass/fail determination against NFR1 before the benchmark document is finalized.
TASK OBJECTIVE (AI PORTION ONLY)
Generate a benchmark-runner script measuring end-to-end image inference latency and per-frame video inference latency across the optimizations from T028/T029; once the human supplies the measured numbers, compile docs/performance-benchmark.md with an explicit pass/fail statement against NFR1.
INPUT CONTEXT
Current files: image_pipeline.py (T021), video_pipeline.py (T022–T024), deeplabv3_backend.py (T019/T029), export_torchscript.py (T028). Human input: measured latency numbers on GPU and CPU.
FILES TO CREATE
/scripts/benchmark_latency.py, /docs/performance-benchmark.md
FILES TO MODIFY
None.
IMPLEMENTATION REQUIREMENTS
benchmark_latency.py: runs N repeated single-image inferences and logs mean/P95 latency; runs a short video through video_pipeline.py and logs mean per-frame latency and FPS; supports a flag to select eager, TorchScript (T028), and FP16 (T029) modes for comparison; no hardcoded sample counts (configurable). performance-benchmark.md: documents GPU and CPU results for each mode, compiled only from human-confirmed measured numbers, with an explicit pass/fail statement against the NFR1 <3s target and any follow-up notes if the target is not met.
ENGINEERING STANDARDS
No fabricated or estimated latency numbers. No silent omission of a failing result.
CONSTRAINTS
Do not modify the pipelines or model backend being benchmarked. Do not soften or omit a failing NFR1 result — if the target is missed, state that explicitly.

ACCEPTANCE CRITERIA

 Documented latency numbers on both GPU and CPU, human-confirmed.
 Explicit pass/fail statement against the NFR1 <3s target.
EXIT CRITERIA
✓ Benchmark script runs to completion on both GPU and CPU ✓ Human-confirmed numbers recorded ✓ No lint errors ✓ Explicit pass/fail documented ✓ Commit ready
TESTING REQUIREMENTS
Manual (human): execute benchmark_latency.py on both GPU and CPU hardware across eager/TorchScript/FP16 modes and supply the resulting numbers for documentation.

OUTPUT FORMAT

Summary 2. Files Created 3. Files Modified 4. Why each file changed 5. Implementation Notes 6. Testing Performed 7. Known Limitations 8. Suggested Git Commit Message 9. Next Recommended Task
====================================================

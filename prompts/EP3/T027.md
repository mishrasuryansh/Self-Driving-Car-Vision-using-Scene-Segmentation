T027 — Run evaluation on the held-out validation split and record baseline
Epic: EP2 | Feature: F2.7 | Milestone: M2
TASK METADATA
Risk Level: High — this baseline is the reference point every later model change is judged against; an execution or recording error propagates into Section 3.7 reporting and the final report (T109).
AI Difficulty: N/A (human-executed) / Easy for AI script-generation portion.
Expected LOC: ~40 (evaluation-runner script)
Expected Files Created: 2 | Expected Files Modified: 0
CURRENT REPOSITORY STATE
Completed: T001–T026 (checkpoint human-verified via T018/T019; metrics.py complete via T025/T026). Relevant modules: metrics.py, deeplabv3_backend.py, prepare_dataset.py.
PROJECT CONTEXT
Requires running the full fine-tuned model against the complete held-out validation split (not a small spot-check batch) — a GPU-bound, dataset-dependent execution step.
HUMAN EXECUTION REQUIRED
A human must: (1) run the AI-generated evaluation-runner script against the full held-out validation split from T015 using the human-verified checkpoint from T018/T019, (2) review the resulting mIoU/pixel-accuracy numbers for plausibility, (3) confirm the recorded baseline is accurate before it is cited elsewhere (e.g., T030, T109).
TASK OBJECTIVE (AI PORTION ONLY)
Generate an evaluation-runner script that iterates the full held-out validation split, computes T025/T026's metrics per batch, aggregates them, and writes the results into a baseline record; once the human confirms the run's output, compile docs/model-evaluation-baseline.md from those human-confirmed numbers.
INPUT CONTEXT
Current files: metrics.py (T025/T026), prepare_dataset.py (T015), deeplabv3_backend.py (T019). Human input: confirmed evaluation-run output values.
FILES TO CREATE
/scripts/run_evaluation.py, /docs/model-evaluation-baseline.md
FILES TO MODIFY
None.
IMPLEMENTATION REQUIREMENTS
run_evaluation.py: iterates the val DataLoader, runs inference, computes per-batch metrics, aggregates a final mIoU/pixel-accuracy, logs progress, writes a machine-readable results file (e.g., JSON). model-evaluation-baseline.md: human-readable summary linking the recorded baseline numbers to Section 3.7's success metrics, populated only from the human-confirmed run output — no fabricated or estimated numbers.
ENGINEERING STANDARDS
No fabricated metric values. No silent skipping of validation batches on error (log and count skips explicitly).
CONSTRAINTS
Do not modify metrics.py, prepare_dataset.py, or the model backend. Do not report numbers that have not been human-confirmed.

ACCEPTANCE CRITERIA

 Baseline mIoU/pixel-accuracy recorded from an actual full validation-split run (not a spot-check).
 Numbers linked explicitly to Section 3.7 success metrics.
 Human confirmation of the run's validity is documented.
EXIT CRITERIA
✓ Evaluation script runs to completion ✓ Human-confirmed results recorded ✓ No lint errors ✓ Baseline doc complete ✓ Commit ready
TESTING REQUIREMENTS
Manual (human): execute run_evaluation.py against the full validation split and review output plausibility before it is recorded as the baseline.

OUTPUT FORMAT

Summary 2. Files Created 3. Files Modified 4. Why each file changed 5. Implementation Notes 6. Testing Performed 7. Known Limitations 8. Suggested Git Commit Message 9. Next Recommended Task
====================================================
====================================================

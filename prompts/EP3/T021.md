T021 — Implement image postprocessing (overlay + class stats)

Epic: EP2 | Feature: F2.5 | Milestone: M2

TASK METADATA

Risk Level: Low-Medium — incorrect stats would mislead users but is easy to unit-test.

AI Difficulty: Easy-Medium.

Expected LOC: \~70

Expected Files Created: 0 | Expected Files Modified: 1

CURRENT REPOSITORY STATE

Completed: T001–T020. Relevant modules: image\_pipeline.py (T020, returns raw mask + timing), color\_map.py (T014, overlay function), taxonomy.py (T013, class names).

PROJECT CONTEXT

Combines the raw mask from T020 with T014's color map to produce the actual user-facing output: an overlay image plus per-class pixel percentage stats, matching FR3/FR4.

TASK OBJECTIVE

Extend image\_pipeline.py to postprocess the T020 mask into an overlay image and a per-class pixel-percentage stats dict.

INPUT CONTEXT

Current files: image\_pipeline.py (T020), color\_map.py (T014), taxonomy.py (T013). Reference: Section 11.4–11.5.

FILES TO CREATE

None.

FILES TO MODIFY

/inference-engine/pipeline/image\_pipeline.py

IMPLEMENTATION REQUIREMENTS

Call T014's overlay function on the T020 mask; compute per-class pixel counts via vectorized numpy operations and convert to percentages keyed by taxonomy class name; percentages must sum to \~100%; extend the T011 result dataclass usage (or the function's return tuple) to include the overlay image and stats dict without breaking T020's existing return contract for callers that only need the mask.

ENGINEERING STANDARDS

No per-pixel Python loops for stats computation. No duplicated overlay logic (reuse T014).

CONSTRAINTS

Do not modify color\_map.py, taxonomy.py, or T020's preprocessing/inference logic. Do not implement the video pipeline (T022).



ACCEPTANCE CRITERIA



&#x20;Overlay visually matches expectations on 3 sample images.

&#x20;Stats sum to 100% (within floating-point rounding tolerance).

&#x20;Existing T020 callers are unaffected (backward compatible).

EXIT CRITERIA

✓ Runs on 3 sample images ✓ Unit test on stats-summation passes ✓ No lint errors ✓ N/A docs ✓ Commit ready

TESTING REQUIREMENTS

Manual: visually inspect overlays on 3 sample images. Unit test: synthetic mask with known class proportions, assert stats match expected percentages.



OUTPUT FORMAT



Summary 2. Files Created 3. Files Modified 4. Why each file changed 5. Implementation Notes 6. Testing Performed 7. Known Limitations 8. Suggested Git Commit Message 9. Next Recommended Task

====================================================

====================================================

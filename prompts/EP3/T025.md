T025 — Implement pixel accuracy metric function
Epic: EP2 | Feature: F2.7 | Milestone: M2
TASK METADATA
Risk Level: Medium — an incorrect metric silently misreports model quality to stakeholders.
AI Difficulty: Easy-Medium.
Expected LOC: ~50
Expected Files Created: 1 | Expected Files Modified: 0
CURRENT REPOSITORY STATE
Completed: T001–T024. Relevant modules: label_mapping.py (T016, remapped validation set available), taxonomy.py (T013).
PROJECT CONTEXT
CHANGE FROM V1: this metric must be validated against the actual held-out validation split produced by T015/T016 — not only a hand-checked synthetic example — so the acceptance evidence reflects real data, consistent with Section 3.7's success-metrics requirement.
TASK OBJECTIVE
Function comparing a predicted mask to a ground-truth mask and returning pixel accuracy, verified against the actual T015/T016 held-out validation split (in addition to a synthetic sanity check).
INPUT CONTEXT
Current files: label_mapping.py (T016), prepare_dataset.py (T015, provides the val split). Reference: Section 3.7, Section 4 SRS.
FILES TO CREATE
/inference-engine/pipeline/metrics.py
FILES TO MODIFY
None.
IMPLEMENTATION REQUIREMENTS
Vectorized numpy implementation (no per-pixel loop); accepts two equally-shaped class-index mask arrays; returns a float in [0, 1]; typed exception on shape mismatch.
ENGINEERING STANDARDS
No per-pixel Python loops. No hardcoded class counts (read from taxonomy.py).
CONSTRAINTS
Do not implement mIoU here (T026). Do not modify label_mapping.py or prepare_dataset.py.

ACCEPTANCE CRITERIA

 Returns 1.0 for identical masks.
 Correct fractional value on a hand-checked synthetic test case.
 Additionally computed against at least a small batch from the actual T015/T016 held-out validation split, with the resulting value logged (not just the synthetic check).
EXIT CRITERIA
✓ Unit test (synthetic) passes ✓ Validation-split spot-check executed and logged ✓ No lint errors ✓ N/A docs ✓ Commit ready
TESTING REQUIREMENTS
Unit test: hand-checked synthetic mask pair. Manual: run against a small batch of the real held-out validation split (T015) and log the resulting accuracy value.

OUTPUT FORMAT

Summary 2. Files Created 3. Files Modified 4. Why each file changed 5. Implementation Notes 6. Testing Performed 7. Known Limitations 8. Suggested Git Commit Message 9. Next Recommended Task
====================================================
====================================================

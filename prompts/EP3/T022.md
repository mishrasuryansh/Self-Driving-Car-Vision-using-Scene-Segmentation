T022 — Port frame-by-frame inference loop into inference-engine
Epic: EP2 | Feature: F2.6 | Milestone: M2
TASK METADATA
Risk Level: Medium — the legacy per-run model reload bug (Section 2.4) must not be reintroduced.
AI Difficulty: Medium.
Expected LOC: ~130
Expected Files Created: 1 | Expected Files Modified: 0
CURRENT REPOSITORY STATE
Completed: T001–T021. Relevant modules: image_pipeline.py (T021, per-frame postprocessing logic to reuse), deeplabv3_backend.py (T019). Legacy reference: video_processor.py (read→infer→overlay loop, reload-per-run bug, missing utils imports).
PROJECT CONTEXT
Retires two Section 2.4 audit findings: the missing utils/color_map.py/utils/fps_counter.py imports that made the legacy script non-runnable, and the model being reloaded on every run.
TASK OBJECTIVE
Reusable video_pipeline.py implementing the read→infer→overlay loop from the legacy video_processor.py, using a model instance loaded once and passed in (not reloaded per frame or per run).
INPUT CONTEXT
Current files: image_pipeline.py (T021, reuse its per-frame postprocessing logic where possible), deeplabv3_backend.py (T019). Reference: legacy video_processor.py (loop structure only — not its bugs).
FILES TO CREATE
/inference-engine/pipeline/video_pipeline.py
FILES TO MODIFY
None.
IMPLEMENTATION REQUIREMENTS
Function/class accepting a video path and an already-loaded T019 backend instance; reads frames via OpenCV; runs inference per frame reusing T021's overlay/stats logic where structurally applicable; writes an overlaid output video; structured logging for progress (e.g., every N frames); typed exception handling for unreadable/corrupt video files (matching the legacy script's cap.isOpened() check, upgraded to raise rather than silently return).
ENGINEERING STANDARDS
No per-frame model reload. No duplicated overlay/stats logic — reuse T021/T014 where the code is structurally identical.
CONSTRAINTS
Do not implement the FPS counter here (T023). Do not implement/remove the simulated car-speed metric here (T024). Do not modify image_pipeline.py, deeplabv3_backend.py, or color_map.py.

ACCEPTANCE CRITERIA

 Given a short sample video, produces an overlaid output video.
 Model is not reloaded per frame (verified via a single "model loaded" log line for the whole run).
EXIT CRITERIA
✓ Runs end-to-end on a short sample video ✓ Manual verification of single model-load log line ✓ No lint errors ✓ N/A docs ✓ Commit ready
TESTING REQUIREMENTS
Manual: run against a short (few-second) sample video from T010's assets or a locally provided clip; confirm output video is produced and the model-load log appears exactly once.

OUTPUT FORMAT

Summary 2. Files Created 3. Files Modified 4. Why each file changed 5. Implementation Notes 6. Testing Performed 7. Known Limitations 8. Suggested Git Commit Message 9. Next Recommended Task
====================================================
====================================================

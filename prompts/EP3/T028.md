T028 — Export the model to TorchScript
Epic: EP2 | Feature: F2.8 | Milestone: M2
TASK METADATA
Risk Level: Medium — requires the real human-verified checkpoint; a broken export silently degrades production inference.
AI Difficulty: Medium.
Expected LOC: ~50 (AI script-generation portion)
Expected Files Created: 1 | Expected Files Modified: 0
CURRENT REPOSITORY STATE
Completed: T001–T027. Relevant modules: deeplabv3_backend.py (T019, fine-tuned checkpoint loading).
PROJECT CONTEXT
Meets Section 11.7's TorchScript-export optimization requirement toward NFR1 latency. This step operates on the real, human-verified checkpoint and produces a new deployable artifact.
HUMAN EXECUTION REQUIRED
A human must run the AI-generated export script against the human-verified checkpoint (T018/T019) and confirm the resulting scripted model produces output matching the eager-mode model before it is treated as usable.
TASK OBJECTIVE (AI PORTION ONLY)
Generate an export script that converts the fine-tuned DeepLabV3 model to TorchScript and a verification routine comparing scripted vs. eager-mode output on a sample batch.
INPUT CONTEXT
Current files: deeplabv3_backend.py (T019). Human-provided: verified checkpoint (T018).
FILES TO CREATE
/scripts/export_torchscript.py
FILES TO MODIFY
None.
IMPLEMENTATION REQUIREMENTS
Load the fine-tuned model via T019's backend; trace/script it via torch.jit.script or torch.jit.trace (whichever is compatible with the model's control flow — document the choice); save the scripted model to a configurable output path; include a verification routine running both eager and scripted versions on the same sample input and comparing outputs within a numerical tolerance; log any discrepancy above tolerance as an error, not a silent pass.
ENGINEERING STANDARDS
No hardcoded checkpoint/output paths — configurable. No silent tolerance failures.
CONSTRAINTS
Do not modify deeplabv3_backend.py. Do not implement FP16 here (T029).

ACCEPTANCE CRITERIA

 Scripted model produces near-identical output to eager mode on a sample batch (human-confirmed).
 Measurable latency improvement logged.
EXIT CRITERIA
✓ Export script runs to completion ✓ Human-confirmed output-parity check passes ✓ No lint errors ✓ N/A docs ✓ Commit ready
TESTING REQUIREMENTS
Manual (human): run the export script against the real checkpoint, confirm output parity and record the latency delta between eager and scripted inference.

OUTPUT FORMAT

Summary 2. Files Created 3. Files Modified 4. Why each file changed 5. Implementation Notes 6. Testing Performed 7. Known Limitations 8. Suggested Git Commit Message 9. Next Recommended Task
====================================================
====================================================

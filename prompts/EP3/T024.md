T024 — Remove/replace the "simulated car speed" placeholder metric
Epic: EP2 | Feature: F2.6 | Milestone: M2
TASK METADATA
Risk Level: Low — scoped, isolated change to a single non-critical HUD element.
AI Difficulty: Easy.
Expected LOC: ~15 (removal) or ~40 (if replaced with a labeled real metric — see Human Checkpoint)
Expected Files Created: 0 | Expected Files Modified: 1
CURRENT REPOSITORY STATE
Completed: T001–T023. Relevant modules: video_pipeline.py (T022/T023). Legacy reference: video_processor.py's centroid-movement "car speed" simulation (Section 2.4 audit finding — not part of Master Doc scope, not a real speed measurement).
PROJECT CONTEXT
Section 2.4 flags the legacy "car speed" HUD value as a simulated, non-physical metric (mask-centroid pixel movement, not real-world speed) that risks misleading users if carried into the product unlabeled.
HUMAN CHECKPOINT REQUIRED
Per the Project Constitution, this decision affects product-facing output and is not purely mechanical. Default AI recommendation: remove the simulated car-speed HUD element entirely, since it is not part of any Master Documentation requirement (FR1–FR9) and has no basis in real vehicle telemetry. A human must confirm this removal (or explicitly request a differently-scoped replacement) before this task is considered complete.
TASK OBJECTIVE
Remove the simulated car-speed metric from video_pipeline.py's HUD output, per the human-confirmed decision above, and document the removal rationale in a code comment.
INPUT CONTEXT
Current files: video_pipeline.py (T022/T023).
FILES TO CREATE
None.
FILES TO MODIFY
/inference-engine/pipeline/video_pipeline.py
IMPLEMENTATION REQUIREMENTS
Remove the centroid-tracking car-speed computation and its HUD text rendering; leave a single code comment explaining that this metric was removed as an unlabeled placeholder per Section 2.4 and the human decision above; do not leave any dead/commented-out code beyond that one explanatory comment.
ENGINEERING STANDARDS
No dead code left behind. No unlabeled "simulated" metrics remain anywhere in the product-facing pipeline.
CONSTRAINTS
Do not modify fps_counter.py or the model-load/overlay logic from T022/T023. Do not introduce a new, unrequested metric in its place.

ACCEPTANCE CRITERIA

 No unlabeled "simulated" metrics remain in the product-facing pipeline.
 Decision (removal) is documented in a code comment.
EXIT CRITERIA
✓ Pipeline still runs end-to-end after removal ✓ N/A tests ✓ No lint errors ✓ Decision documented ✓ Commit ready — pending human confirmation noted above
TESTING REQUIREMENTS
Manual: re-run the T022 sample-video test and confirm the pipeline still completes successfully with the car-speed HUD element absent.

OUTPUT FORMAT

Summary 2. Files Created 3. Files Modified 4. Why each file changed 5. Implementation Notes 6. Testing Performed 7. Known Limitations 8. Suggested Git Commit Message 9. Next Recommended Task
====================================================
====================================================

T023 — Port FPS counter utility into the engine package
Epic: EP2 | Feature: F2.6 | Milestone: M2
TASK METADATA
Risk Level: Low — direct, unmodified port of proven logic.
AI Difficulty: Easy.
Expected LOC: ~20
Expected Files Created: 1 | Expected Files Modified: 1
CURRENT REPOSITORY STATE
Completed: T001–T022. Relevant modules: video_pipeline.py (T022, currently has no FPS reporting). Legacy reference: fps_counter.py (proven, working FPSCounter class).
PROJECT CONTEXT
Closes the "missing utils import" audit finding (Section 2.4) for the FPS counter specifically; the legacy FPSCounter logic is already correct and only needs relocating into the new package structure.
TASK OBJECTIVE
Port the legacy fps_counter.py logic unchanged in behavior into the inference-engine package, and wire it into video_pipeline.py's per-frame loop.
INPUT CONTEXT
Current files: legacy fps_counter.py (reference for exact behavior to preserve), video_pipeline.py (T022).
FILES TO CREATE
/inference-engine/pipeline/fps_counter.py
FILES TO MODIFY
/inference-engine/pipeline/video_pipeline.py
IMPLEMENTATION REQUIREMENTS
Port the FPSCounter class (update/get_fps behavior) unchanged; instantiate it once in video_pipeline.py's loop (not per frame) and log the current FPS at the same cadence the legacy script used (every ~1 second of wall-clock time).
ENGINEERING STANDARDS
No behavioral changes to the ported logic — this is a pure relocation.
CONSTRAINTS
Do not change the FPS calculation algorithm. Do not touch T024's scope (car-speed metric) in this task.

ACCEPTANCE CRITERIA

 FPS values match prior CLI behavior on the same sample video within rounding.
EXIT CRITERIA
✓ FPS values logged during a sample run ✓ N/A automated tests (behavior-preservation verified manually) ✓ No lint errors ✓ N/A docs ✓ Commit ready
TESTING REQUIREMENTS
Manual: run the same sample video through both the legacy script (for reference, if runnable) and the new pipeline, and compare logged FPS values for rough parity.

OUTPUT FORMAT

Summary 2. Files Created 3. Files Modified 4. Why each file changed 5. Implementation Notes 6. Testing Performed 7. Known Limitations 8. Suggested Git Commit Message 9. Next Recommended Task
====================================================
====================================================

T004 — Author docker-compose.yml wiring all services + Redis + DB
Epic: EP1 | Feature: F1.2 | Milestone: M1
TASK METADATA
Risk Level: Medium — misconfigured networking/volumes blocks every downstream task.
AI Difficulty: Medium.
Expected LOC: ~120
Expected Files Created: 1 | Expected Files Modified: 0
CURRENT REPOSITORY STATE
Completed: T001–T003. Relevant modules: 4 Dockerfiles. Known tech debt: none.
PROJECT CONTEXT
Assembles the full local topology (Section 6.5–6.6, 12.2) into one compose file.
TASK OBJECTIVE
docker-compose.yml wiring frontend, backend, worker, inference-engine, db, redis on a shared network with named volumes.
INPUT CONTEXT
Current files: 4 Dockerfiles (T003). Dependencies: official Redis image; MongoDB image (Section 9 default).
FILES TO CREATE
/infra/docker-compose.yml
FILES TO MODIFY
None.

IMPLEMENTATION REQUIREMENTS

6 services; app services build from T003 Dockerfiles (not prebuilt images).
db/redis: pinned versions, not :latest.
Shared user-defined network; named volumes for db data + storage/uploads + storage/outputs.
Health checks for db/redis.
ENGINEERING STANDARDS
No hardcoded secrets — reference env vars by name only (values come in T007).
CONSTRAINTS
Do not create .env (T007). Do not add app code. Do not add services outside Section 6.

ACCEPTANCE CRITERIA

 docker compose config validates.
 docker compose up starts all 6 without crash-looping.
 Services reach each other by name.
EXIT CRITERIA
✓ Compose validates + runs ✓ N/A tests ✓ No lint errors (yamllint-clean) ✓ N/A docs ✓ Commit ready
TESTING REQUIREMENTS
Manual: docker compose up; exec into one container and curl/ping another by service name.

OUTPUT FORMAT

Summary 2. Files Created 3. Files Modified 4. Why each file changed 5. Implementation Notes 6. Testing Performed 7. Known Limitations 8. Suggested Git Commit Message 9. Next Recommended Task
====================================================

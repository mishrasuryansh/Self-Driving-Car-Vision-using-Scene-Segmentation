T009 — Evaluate DeepLabV3 fine-tune vs YOLOv8-Seg (configurable decision)

Epic: EP1 | Feature: F1.5 | Milestone: M1

TASK METADATA

Risk Level: Medium — wrong or hard-locked choice affects all of Epic 2.

AI Difficulty: Medium.

Expected LOC: ~30 (config scaffold) + doc

Expected Files Created: 2 | Expected Files Modified: 0

CURRENT REPOSITORY STATE

Completed: T001–T008. Relevant modules: none in inference-engine/ yet.

PROJECT CONTEXT

CHANGE FROM V1: the decision must now be a configurable value, not a permanently hardcoded choice baked into downstream code — so the backbone can be swapped later without a redesign.

TASK OBJECTIVE

Produce a comparison document (DeepLabV3 fine-tune vs YOLOv8-Seg) against NFR1 latency, the Section 4.8 GPU constraint, and Cityscapes/BDD100K compatibility, AND scaffold a MODEL_BACKEND config value/enum that downstream code will read rather than hardcoding the chosen model.

INPUT CONTEXT

Current files: none. Reference: Master Doc Sections 3.4, 4.8, 11.

FILES TO CREATE

/docs/model-decision.md, /inference-engine/config.py (defines a ModelBackend enum/config key, defaulted to the recommended choice)

FILES TO MODIFY

None.



IMPLEMENTATION REQUIREMENTS



Doc: candidate summary, comparison table, recommended backbone, rationale, fallback plan.

Config: an enum/constant (ModelBackend.DEEPLABV3 / .YOLOV8_SEG) read by future model-loading code — no downstream file may hardcode a model name string directly.

ENGINEERING STANDARDS

No magic strings for model selection anywhere — always through this config.

CONSTRAINTS

Do not implement either model. Do not introduce a third option beyond Section 11.



ACCEPTANCE CRITERIA



 Doc states a clear recommended backbone with rationale + fallback.

 Config scaffold makes the choice swappable without code changes elsewhere.

HUMAN CHECKPOINT REQUIRED

The AI-recommended backbone in model-decision.md must be reviewed and explicitly confirmed by a human (supervisor/team) before T012 proceeds. Do not treat the AI's recommendation as final.

EXIT CRITERIA

✓ N/A build ✓ N/A tests ✓ No lint errors ✓ Doc reviewable ✓ Commit ready — pending human sign-off noted above

TESTING REQUIREMENTS

Manual: human review confirms the recommendation and rationale are sound.



OUTPUT FORMAT



Summary 2. Files Created 3. Files Modified 4. Why each file changed 5. Implementation Notes 6. Testing Performed 7. Known Limitations 8. Suggested Git Commit Message 9. Next Recommended Task

====================================================

====================================================

T060 — Add global layout, nav bar, and mobile hamburger behavior
+Epic: EP5 | Feature: F5.1 | Milestone: M5
+TASK METADATA
+Risk Level: Low. AI Difficulty: Medium (responsive breakpoint behavior requires care).
+Expected LOC: ~100
+Expected Files Created: 2 | Expected Files Modified: 0
+CURRENT REPOSITORY STATE
+Completed: T001–T059. Relevant modules: /frontend/src/App.tsx and all placeholder pages (T059, minimal nav links).
+PROJECT CONTEXT
+Section 10.2 specifies the exact persistent nav bar and its mobile-collapse behavior, used by every page built afterward (T063 onward).
+TASK OBJECTIVE
+A persistent nav bar per Section 10.2, collapsing to a hamburger menu on mobile, plus a shared page-layout wrapper.
+INPUT CONTEXT
+Current files: App.tsx and routing (T059). Reference: Section 10.2 (Logo/Home, Dashboard, Upload dropdown, History, Analytics, Settings, About, auth control; mobile breakpoint collapses to hamburger).
+FILES TO CREATE
+/frontend/src/components/NavBar.tsx, /frontend/src/components/Layout.tsx
+FILES TO MODIFY
+None.
+IMPLEMENTATION REQUIREMENTS
+NavBar.tsx: renders exactly the items listed in Section 10.2, with the "Upload" item as a dropdown/menu offering Image and Video sub-routes; collapses into a hamburger menu below the documented mobile breakpoint (~768px). Layout.tsx: a shared wrapper applying the nav bar consistently around the routed page content from T059, so every future page (T063 onward) is wrapped without re-implementing the nav per page.
+ENGINEERING STANDARDS
+No duplicated nav-rendering logic per page — every route uses the single Layout.tsx wrapper.
+CONSTRAINTS
+Do not implement the auth control's real behavior yet (that depends on T062's AuthContext, built later) — a placeholder auth-control slot is acceptable here, to be wired in T062/T064. Do not modify any page component's internal content.
+
+ACCEPTANCE CRITERIA
+
+ Nav bar matches Section 10.2 items exactly.
+ Collapses correctly to a hamburger menu under ~768px viewport width.
+EXIT CRITERIA
+✓ Manual desktop and mobile-width verification passes ✓ No lint errors ✓ N/A docs ✓ Commit ready
+TESTING REQUIREMENTS
+Manual: view the app at desktop width and confirm all Section 10.2 nav items render correctly; resize/emulate a mobile viewport (~375px) and confirm the nav collapses into a functional hamburger menu.
+
+OUTPUT FORMAT
+
+Summary 2. Files Created 3. Files Modified 4. Why each file changed 5. Implementation Notes 6. Testing Performed 7. Known Limitations 8. Suggested Git Commit Message 9. Next Recommended Task
+

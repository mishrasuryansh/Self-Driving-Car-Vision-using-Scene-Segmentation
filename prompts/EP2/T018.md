T018 — Run fine-tuning and export checkpoint

Epic: EP2 | Feature: F2.4 | Milestone: M2

TASK METADATA

Risk Level: High — GPU cost/time, and the resulting checkpoint drives the entire product's quality.

AI Difficulty: N/A (human-executed).

Expected LOC: ~40 (AI verification script only)

Expected Files Created: 1 | Expected Files Modified: 0 (+ .gitignore check)

CURRENT REPOSITORY STATE

Completed: T001–T017. Relevant modules: train.py (T017, verified working).

PROJECT CONTEXT

CHANGE FROM V1: this is now a manual milestone, not an AI coding task. GPU training execution requires human-provisioned compute (Section 4.8/15.2) and human judgment on when training is sufficient.

HUMAN EXECUTION REQUIRED

A human must: (1) run /scripts/train.py (T017) with production hyperparameters against the full prepared dataset on available GPU compute, (2) save the resulting checkpoint to /inference-engine/weights/model_v1.pt, (3) confirm the .gitignore excludes this path.

TASK OBJECTIVE (AI PORTION ONLY)

Once the human provides the trained checkpoint, generate a verification script that loads it via T012's backend and runs inference on a handful of sample images to confirm validity.

INPUT CONTEXT

Human-provided: /inference-engine/weights/model_v1.pt.

FILES TO CREATE

/scripts/verify_checkpoint.py

FILES TO MODIFY

None (confirm .gitignore already covers inference-engine/weights/; if not, flag it — do not silently edit without noting it).

IMPLEMENTATION REQUIREMENTS

Load checkpoint via T012's backend; run inference on 3–5 sample images; log shape/class-distribution sanity checks; typed exception if the checkpoint fails to load.

ENGINEERING STANDARDS

No training logic in this script — verification only.

CONSTRAINTS

Do not execute training. Do not commit the checkpoint file. Do not wire the checkpoint into the backend by default yet (T019).



ACCEPTANCE CRITERIA



 Checkpoint loads successfully via T012's loader.

 Qualitative predictions look correct on sample images (visually spot-checked by human).

 Checkpoint confirmed git-ignored.

EXIT CRITERIA

✓ Verification script passes ✓ Human sign-off on checkpoint quality ✓ No lint errors ✓ N/A docs ✓ Commit ready (verification script only)

TESTING REQUIREMENTS

Manual: run verification script against the human-provided checkpoint; git status confirms checkpoint untracked.



OUTPUT FORMAT



Summary 2. Files Created 3. Files Modified 4. Why each file changed 5. Implementation Notes 6. Testing Performed 7. Known Limitations 8. Suggested Git Commit Message 9. Next Recommended Task

====================================================

====================================================

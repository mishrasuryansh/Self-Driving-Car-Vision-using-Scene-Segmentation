T053 — Implement POST /api/v1/jobs/video (enqueue only)
+Epic: EP4 | Feature: F4.3 | Milestone: M4
+TASK METADATA
+Risk Level: High — this is the first endpoint that must return quickly regardless of underlying video length, directly implementing NFR2 (async, non-blocking processing).
+AI Difficulty: Medium.
+Expected LOC: ~110
+Expected Files Created: 0 | Expected Files Modified: 1
+CURRENT REPOSITORY STATE
+Completed: T001–T052. Relevant modules: /backend/app/api/v1/jobs.py (T042–T045, image job patterns to mirror for validation/storage/ownership), /backend/app/core/validators.py (T041), /backend/app/services/storage.py (T039), /backend/app/core/queue.py (T049), /worker/app/tasks/contract.py (T050).
+PROJECT CONTEXT
+Implements the async counterpart to T042's synchronous image endpoint — validates and stores the upload, then hands off to the queue instead of blocking on inference, per Section 6.3's async pipeline design.
+TASK OBJECTIVE
+Implement POST /api/v1/jobs/video: validate and store the uploaded video, create a "queued" job row, enqueue a T050-contract task, and return immediately per Section 8.2.
+INPUT CONTEXT
+Current files: jobs.py (T042–T045), validators.py (T041), storage.py (T039), queue.py (T049), contract.py (T050). Reference: Section 8.2 (POST /api/v1/jobs/video — request multipart/form-data {file, modelId?}, response {jobId, status: "queued", createdAt}; validation: mp4 type, ≤200MB, ≤2 minutes; errors: 400, 401, 413).
+FILES TO CREATE
+None.
+FILES TO MODIFY
+/backend/app/api/v1/jobs.py
+IMPLEMENTATION REQUIREMENTS
+Route handler behind T038's auth guard: validate the upload via T041's validate_video_upload, save the original file via T039, persist a Job record with status "queued" via T033's model, build the T050 payload and enqueue it via T049's queue.py, and return the Section 8.2 response shape immediately — no inference call occurs in this request/response cycle.
+ENGINEERING STANDARDS
+Route handler stays thin, mirroring T042's structure for consistency. No duplicated validation/storage logic — reuses T041/T039 directly.
+CONSTRAINTS
+Do not call the video pipeline synchronously in this task. Do not modify validators.py, storage.py, or queue.py. Do not implement the worker-side handler here (T055).
+
+ACCEPTANCE CRITERIA
+
+ Matches Section 8.2 contract.
+ Response returns in well under 1 second regardless of video length.
+EXIT CRITERIA
+✓ Integration test confirms sub-second response time on a large valid video ✓ Enqueue confirmed via the T049 broker connection ✓ No lint errors ✓ N/A docs ✓ Commit ready
+TESTING REQUIREMENTS
+Integration test: authenticated request with a valid MP4 near the size/duration limit returns 200/201 with status "queued" in well under 1 second, and the corresponding task is confirmed present on the broker (or consumed by a stub consumer). Integration test: oversized/wrong-type file returns 400/413 without enqueueing.
+
+OUTPUT FORMAT
+
+Summary 2. Files Created 3. Files Modified 4. Why each file changed 5. Implementation Notes 6. Testing Performed 7. Known Limitations 8. Suggested Git Commit Message 9. Next Recommended Task
+

T056 — Add failure handling and single automatic retry
+Epic: EP4 | Feature: F4.4 | Milestone: M4
+TASK METADATA
+Risk Level: Medium — directly implements Section 4.7's reliability requirement; incorrect retry logic can either mask real failures or cause duplicate processing.
+AI Difficulty: Medium.
+Expected LOC: ~60
+Expected Files Created: 0 | Expected Files Modified: 1
+CURRENT REPOSITORY STATE
+Completed: T001–T055. Relevant modules: /worker/app/tasks/video_task.py (T055, happy path only).
+PROJECT CONTEXT
+Section 4.7 requires: "Failed inference jobs are retried once automatically, then marked Failed with a user-visible reason."
+TASK OBJECTIVE
+A task that throws is retried once automatically, then marked "failed" with an error message, per Section 4.7.
+INPUT CONTEXT
+Current files: video_task.py (T055). Reference: Section 4.7 (Reliability).
+FILES TO CREATE
+None.
+FILES TO MODIFY
+/worker/app/tasks/video_task.py
+IMPLEMENTATION REQUIREMENTS
+Wrap T055's processing logic with the queue library's native retry mechanism configured for exactly one automatic retry; on final failure (after the retry is exhausted), update the job's DB status to "failed" with a human-readable error message (not a raw stack trace) and log the full exception server-side; ensure retry does not duplicate already-written partial output artifacts (clean up or overwrite deterministically on retry).
+ENGINEERING STANDARDS
+Exactly one retry — not zero, not unbounded. No raw stack traces persisted to the user-visible error field.
+CONSTRAINTS
+Do not modify the happy-path logic from T055 beyond adding retry/failure wrapping. Do not modify contract.py.
+
+ACCEPTANCE CRITERIA
+
+ Forced failure on the first attempt succeeds on retry.
+ Failure on both attempts marks the job "failed" with a visible reason.
+EXIT CRITERIA
+✓ Retry-then-succeed test passes ✓ Retry-then-fail test passes ✓ T055's original happy-path test still passes unchanged ✓ No lint errors ✓ N/A docs ✓ Commit ready
+TESTING REQUIREMENTS
+Integration test: inject a failure on the first attempt only (e.g., via a mock that fails once then succeeds), confirm the job ultimately reaches "completed". Integration test: inject failure on both attempts, confirm the job reaches "failed" with a readable error message. Regression: re-run T055's original end-to-end test.
+
+OUTPUT FORMAT
+
+Summary 2. Files Created 3. Files Modified 4. Why each file changed 5. Implementation Notes 6. Testing Performed 7. Known Limitations 8. Suggested Git Commit Message 9. Next Recommended Task
+

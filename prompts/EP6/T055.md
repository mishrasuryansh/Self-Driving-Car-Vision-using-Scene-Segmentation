T055 — Implement the worker task handler calling video_pipeline
+Epic: EP4 | Feature: F4.4 | Milestone: M4
+TASK METADATA
+Risk Level: High — this is the core integration point tying together the queue (T049–T051), the model lifecycle (T052), the video pipeline (Epic 2's T022–T024), and the status contract (T050/T054); it is the single most complex task in Epic 4.
+AI Difficulty: Hard.
+Expected LOC: ~140
+Expected Files Created: 1 | Expected Files Modified: 0
+CURRENT REPOSITORY STATE
+Completed: T001–T054. Relevant modules: /worker/app/main.py (T051/T052, worker with model loaded once), /inference-engine/pipeline/video_pipeline.py (T022–T024, complete video inference pipeline), /worker/app/tasks/contract.py (T050), /backend/app/api/v1/jobs.py (T054, status-read path already verified).
+PROJECT CONTEXT
+Delivers the first true end-to-end async video flow: upload (T053) → queue → worker → inference (Epic 2) → status/result readable via T054.
+TASK OBJECTIVE
+Implement the worker task handler that consumes a T050-contract payload, calls T022's video_pipeline using the T052 pre-loaded model, and writes progress/result back to the database so T054's endpoint reflects it correctly.
+INPUT CONTEXT
+Current files: main.py (T051/T052), video_pipeline.py (T022–T024), contract.py (T050), jobs.py (T054, read-path contract to satisfy). Dependencies: the same database access layer used by the backend (T033's models), accessed from the worker process.
+FILES TO CREATE
+/worker/app/tasks/video_task.py
+FILES TO MODIFY
+None.
+IMPLEMENTATION REQUIREMENTS
+Task handler function matching the queue library's expected task signature: reads the T050 payload, updates the job's DB status to "processing" immediately upon starting, retrieves the input video via T039's storage service, calls T022's video_pipeline using the T052-loaded model instance (never reloading it), saves the resulting output video via T039, updates the job's DB status to "completed" with the resultUrl populated, and does all of this with structured logging at each stage transition.
+ENGINEERING STANDARDS
+No new model instantiation inside this handler — must reuse T052's pre-loaded instance exclusively. No direct filesystem access bypassing T039's storage abstraction.
+CONSTRAINTS
+Do not implement retry/failure handling in this task (T056 — happy path only). Do not implement metrics computation here (T057). Do not modify video_pipeline.py or contract.py.
+
+ACCEPTANCE CRITERIA
+
+ End-to-end: upload video via API (T053) → job transitions queued→processing→completed → resultUrl downloadable and playable.
+EXIT CRITERIA
+✓ Full end-to-end integration test (API → queue → worker → completed status) passes ✓ No lint errors ✓ N/A docs ✓ Commit ready
+TESTING REQUIREMENTS
+Integration test: submit a short sample video via POST /api/v1/jobs/video (T053), allow the worker to process it, poll GET /api/v1/jobs/{jobId} (T054) until "completed", and confirm the resultUrl points to a valid, playable output video.
+
+OUTPUT FORMAT
+
+Summary 2. Files Created 3. Files Modified 4. Why each file changed 5. Implementation Notes 6. Testing Performed 7. Known Limitations 8. Suggested Git Commit Message 9. Next Recommended Task
+
